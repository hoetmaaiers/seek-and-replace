export default class FindAndReplacer {
    constructor() {
        console.log('welcome this FindAndReplace instance');
    }

    replace(path, keyDefinitions) {
        console.log('replace');

        // read every directory in path
        // for every directory check if the regex for variables matches
    }

    
    replaceDirectoryName() {
        // readdirSync();
    }

    replaceFileName() {
    }

    replaceContent() {
    }
}